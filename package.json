{
    "private": true,
    "scripts": {
        "build": "dx build --package lpk-unpack --platform desktop --release",
        "test": "cargo test --release",
        "fmt": "cargo fmt --all",
        "p": "git push && git push --tags --prune",
        "reset": "git reset Head~ --soft",
        "u": "cargo upgrade --workspace"
    }
}
